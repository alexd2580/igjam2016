local Shield = Component.load({"Shield"})

items = {
    shield = {
        name = "Shield",
        component = Shield,
        layer = 2,
        image = resources.images.shield
        -- image, description, component etc.
    }
}
